<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Streaming the 30 AI commentary clips that aren't bundled (see
         audio/AudioPack.kt) and, as a last resort, downloading a sound
         effect if the bundled copy is ever missing. Everything else works
         without a connection: all 167 bundled sounds play from the
         encrypted pack, and anything that truly can't be reached falls
         back to a synthesized sound. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Wicket and boundary buzzes (the timing rhythm uses Compose's own haptics). -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Lets SpokenCommentary find a text-to-speech engine on Android 11+. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
    </queries>

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <!-- configChanges: rotating the phone (or a keyboard or window-size
             change) would otherwise recreate the activity, and a match in
             progress lives only in Compose state, so it would be lost. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|smallestScreenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
